# Backend Python dependencies required by backend.py
# Core web / API
fastapi
uvicorn[standard]

# Data / parsing
beautifulsoup4

# HTTP
requests

# Selenium for browser automation
selenium
# Optional helpers for providing a chromedriver binary or manager (Windows):
chromedriver-binary
webdriver-manager

# OpenAI API client for ChatGPT
openai

# Environment variables and config
python-dotenv

# Typed models
pydantic

# Encryption for secure local credential storage
cryptography

# Note: The project imports `from bs4 import BeautifulSoup` (provided by beautifulsoup4)
# and `from google import genai` (provided by google-genai). Selenium still requires a
# matching Chrome/Chromium and ChromeDriver on PATH or using a manager like webdriver-manager.